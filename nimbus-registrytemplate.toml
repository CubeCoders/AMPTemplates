bind_url = "{{bind_url}}"
shared_secret = "{{shared_secret}}"
accepted_secrets = [{{accepted_secrets}}]
backend_stale_seconds = {{backend_stale_seconds}}
backend_drop_seconds = {{backend_drop_seconds}}
nonce_window_seconds = {{nonce_window_seconds}}
max_reservation_ttl_seconds = {{max_reservation_ttl_seconds}}
log_heartbeats = {{log_heartbeats}}
state_dir = "{{state_dir}}"
{{Extra_root}}
[api_tokens]
enabled = {{api_tokens_enabled}}
rate_limit_per_minute = {{api_tokens_rate_limit_per_minute}}
trust_forwarded_proto = {{api_tokens_trust_forwarded_proto}}
{{Extra_api_tokens}}
[identity]
advertise_on_master_server = {{identity_advertise_on_master_server}}
master_server_url = "{{identity_master_server_url}}"
public_host = "{{identity_public_host}}"
public_port = {{identity_public_port}}
server_name = "{{identity_server_name}}"
server_description = "{{identity_server_description}}"
server_url = "{{identity_server_url}}"
server_icon = "{{identity_server_icon}}"
vh_identifier = "{{identity_vh_identifier}}"
game_version = "{{identity_game_version}}"
max_players_override = {{identity_max_players_override}}
mod_source = "{{identity_mod_source}}"
whitelisted = {{identity_whitelisted}}
has_password = {{identity_has_password}}
heartbeat_interval_seconds = {{identity_heartbeat_interval_seconds}}
{{Extra_identity}}
[identity.playstyle]
id = "{{identity_playstyle_id}}"
langcode = "{{identity_playstyle_langcode}}"
{{Extra_identity_playstyle}}
{{UserSettings}}
